{
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "yarn build && gh-pages -d docs/.vuepress/dist"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "vuepress": "^1.2.0"
  }
}
