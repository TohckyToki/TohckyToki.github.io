{
  "scripts": {
    "dev": "node ./builds/addMarkdown.js && vuepress dev docs",
    "build": "node ./builds/addMarkdown.js && vuepress build docs && node ./builds/delMarkdown.js"
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.2.0",
    "vuepress": "^1.2.0"
  }
}
